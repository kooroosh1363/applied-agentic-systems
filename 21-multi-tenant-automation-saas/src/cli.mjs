import fs from'node:fs';import*as c from'./core.mjs';const x=JSON.parse(fs.readFileSync(process.argv[2]||'examples/demo.json')),s=c.createState({clock:()=> '2026-01-01T00:00:00.000Z'}),t=c.provisionTenant(s,x.tenant);for(const m of x.members)c.addMember(s,{tenantId:t.id,actorId:x.tenant.ownerUserId,...m});const runs=x.runs.map(r=>c.enqueueRun(s,{tenantId:t.id,...r}));console.log(JSON.stringify({tenant:t,runs,auditVerified:c.verifyAudit(s,t.id),spendAuthorized:false,autoPublish:false},null,2));
