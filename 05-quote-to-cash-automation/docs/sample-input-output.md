# Sample input/output

`examples/standard-quote.json` calculates subtotal 125000 minor units, 6250 discount, 5938 tax, and 124688 total under the illustrative line-tax policy and needs no approval. `high-value-quote.json` requires sales manager, finance, and potentially executive approval based on thresholds. `invalid-quote.json` is rejected. Payment and cash evidence remain simulated until a signed provider adapter is integrated.
