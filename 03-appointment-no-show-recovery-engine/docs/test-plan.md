# Test plan

Automated coverage includes normalization, invalid data, stable identity, slot keys, half-open overlap semantics, available/conflict/late routing, consented reminders, waitlist priority and FIFO, opt-out and recovery cap, completed-only revenue, lifecycle transitions, delivery retry cap, HTTP endpoints, workflow graph integrity, required SQL reliability controls, repository JSON/secret scans, and CI Compose rendering. Real-provider E2E, timezone/DST property tests, load, soak, and restore drills remain production gates.
