import{readFile}from'node:fs/promises';import{analyzeBatch,buildExportManifest}from'./core.mjs';const file=process.argv[2];if(!file)throw new Error('usage: node src/cli.mjs <feedback-batch.json>');const input=JSON.parse(await readFile(file,'utf8'));const report=analyzeBatch(input.feedback,input.options);console.log(JSON.stringify({report,manifest:buildExportManifest(report)},null,2));
